# 本周计划

从周三开始，看完算法的图章节，还有两章，产出是最小生成树的C++代码，和最短路径的C++代码，成果是能够默写出这些代码

所以

周四需要看完最小生成树

周五先默写最小生成树代码，然后看完最短路径

周六默写最小生成树代码和最短路径代码，然后看完字符串排序和单词查找树

周日看完字符串，稍微有点赶。

先大致是这样。

看算法就是一边看，一边把java代码转换成C++代码。

写C++代码的顺序是，先从功能思考API接口，然后实现API接口，实现的过程中需要什么私有成员就加上去。